# Quality Insights Backend

FastAPI backend for quality insights.

## MySQL (XAMPP)

1. Start MySQL in XAMPP.
2. Create database `quality_insights`.
3. Create `.env` with:

```env
DATABASE_URL=mysql+pymysql://root:@localhost:3306/quality_insights
```
